clear all
close all

file = './src/2FSK KV/13.4199MHz_20161109_122803.binIQ';

fileID = fopen(file, 'r', 'b');
s = dir(file);

ALL = fread(fileID, s.bytes, 'uint8');

for i = 1:s.bytes/2
    test(i) = i;
    test(i) = i;
%     y1=real(cmplxx(i))*cos(2*pi*fs*t); % inphase component
%     y2=real(cmplxx(i))*sin(2*pi*fs*t) ;% Quadrature component
%     I=[I y1]; % inphase signal vector
%     Q=[Q y2]; %quadrature signal vector
%     y=[y y1+y2]; % modulated signal vector
end